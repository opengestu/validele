# Copie ce fichier en `keystore.properties` (NE PAS COMMIT)
# Place ton fichier .jks dans `android/` (recommandé)
# Exemple : android/validele-release.jks

storeFile=validele-release.jks
storePassword=CHANGE_ME
keyAlias=validele
keyPassword=CHANGE_ME
